// Exportaciones de componentes WhatsApp
export { default as WhatsAppButton } from '../WhatsAppButton';
export { default as WhatsAppInlineButton } from '../WhatsAppInlineButton';

// Hook de WhatsApp
export { default as useWhatsApp } from '../../hooks/useWhatsApp';

// Configuración
export { whatsappConfig } from '../../config/whatsapp';
